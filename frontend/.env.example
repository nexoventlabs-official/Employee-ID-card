# Leave EMPTY for local dev — the Vite proxy in vite.config.js forwards /api to localhost:5000.
# For production (Vercel), set this to your Render backend URL.
# Example:
# VITE_API_URL=https://employee-id-card-backend.onrender.com
VITE_API_URL=https://employee-id-card.onrender.com
